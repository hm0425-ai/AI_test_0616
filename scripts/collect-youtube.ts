/**
 * 하루 1회 실행되는 영상 수집 cron 스크립트 (2단계에서 구현).
 * Vercel Cron 또는 Railway cron에서 `npm run collect:youtube`로 실행.
 */
async function main() {
  console.log("[collect-youtube] 2단계에서 YouTube Data API 연동 후 구현됩니다.");
}

main();
