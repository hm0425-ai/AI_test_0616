export function SiteFooter() {
  return (
    <footer className="border-t border-border py-8 text-center text-sm text-muted-foreground">
      <p>© {new Date().getFullYear()} 보듬TV 펫AI. 모든 답변은 보듬TV 영상을 기반으로 제공됩니다.</p>
    </footer>
  );
}
